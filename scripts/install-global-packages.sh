echo "check installed global"
echo "https://docs.npmjs.com/updating-packages-downloaded-from-the-registry"
npm ls -g

npm install prettier -g
npm install jest -g
npm install typescript -g
npm install eslint -g
npm install next-sitemap -g
npm install husky -g
npm install corepack -g
npm install create-react-app -g 
npm install npm-check-updates -g 
npm install pm2 -g 
npm install tar -g 
npm install typescript -g 
npm install uuid@latest -g 

